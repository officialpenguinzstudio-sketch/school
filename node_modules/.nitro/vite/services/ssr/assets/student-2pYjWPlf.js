import { jsxs, jsx, Fragment } from "react/jsx-runtime";
import { useNavigate, Link } from "@tanstack/react-router";
import { motion, AnimatePresence } from "framer-motion";
import { useEffect, useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { u as useAuth, a as useServerFn } from "./router-BkKrKimK.js";
import { LogOut, FileText, Clock, CheckCircle2, Trophy, PlayCircle } from "lucide-react";
import { B as Button } from "./button-DjOZMqFS.js";
import { D as Dialog, a as DialogContent, b as DialogHeader, c as DialogTitle, d as DialogDescription } from "./dialog-DNrxlBog.js";
import { l as listPublishedForms, g as getMySubmissions, a as getQuestionCount, b as getLeaderboard } from "./submissions.functions-YOHQta7i.js";
import "./server-LI3vKvOL.js";
import "node:async_hooks";
import "h3-v2";
import "@tanstack/router-core";
import "seroval";
import "@tanstack/history";
import "@tanstack/router-core/ssr/client";
import "@tanstack/router-core/ssr/server";
import "@tanstack/react-router/ssr/server";
import "zod";
import "sonner";
import "@radix-ui/react-slot";
import "class-variance-authority";
import "clsx";
import "tailwind-merge";
import "@radix-ui/react-dialog";
function StudentDashboard() {
  const {
    user,
    loading,
    logout
  } = useAuth();
  const navigate = useNavigate();
  const fetchForms = useServerFn(listPublishedForms);
  const fetchSubs = useServerFn(getMySubmissions);
  useEffect(() => {
    if (loading) return;
    if (!user) navigate({
      to: "/"
    });
    else if (user.role !== "student") navigate({
      to: "/admin"
    });
  }, [user, loading, navigate]);
  const {
    data: forms = []
  } = useQuery({
    queryKey: ["forms-published"],
    enabled: !!user && user.role === "student",
    queryFn: () => fetchForms()
  });
  const {
    data: submissions = []
  } = useQuery({
    queryKey: ["my-subs"],
    enabled: !!user && user.role === "student",
    queryFn: () => fetchSubs()
  });
  const completedIds = new Set(submissions.map((s) => s.form_id));
  const available = forms.filter((f) => !completedIds.has(f.id));
  const completed = forms.filter((f) => completedIds.has(f.id));
  const [selected, setSelected] = useState(null);
  if (!user) return null;
  return /* @__PURE__ */ jsxs("main", { className: "min-h-screen px-4 py-8 sm:px-8", children: [
    /* @__PURE__ */ jsxs("header", { className: "mx-auto flex max-w-6xl items-center justify-between", children: [
      /* @__PURE__ */ jsxs("div", { children: [
        /* @__PURE__ */ jsx("p", { className: "text-xs uppercase tracking-widest text-muted-foreground", children: "Welcome back" }),
        /* @__PURE__ */ jsx("h1", { className: "text-2xl font-bold sm:text-3xl", children: user.name })
      ] }),
      /* @__PURE__ */ jsxs(Button, { variant: "ghost", onClick: async () => {
        await logout();
        navigate({
          to: "/"
        });
      }, className: "gap-2", children: [
        /* @__PURE__ */ jsx(LogOut, { className: "h-4 w-4" }),
        " Sign out"
      ] })
    ] }),
    /* @__PURE__ */ jsxs("section", { className: "mx-auto mt-10 max-w-6xl", children: [
      /* @__PURE__ */ jsx("h2", { className: "mb-4 text-sm font-semibold uppercase tracking-widest text-muted-foreground", children: "Available" }),
      available.length === 0 ? /* @__PURE__ */ jsx("div", { className: "glass rounded-2xl p-8 text-center text-muted-foreground", children: "No quizzes available right now." }) : /* @__PURE__ */ jsx("div", { className: "grid gap-4 sm:grid-cols-2 lg:grid-cols-3", children: available.map((f, i) => /* @__PURE__ */ jsxs(motion.button, { initial: {
        opacity: 0,
        y: 16
      }, animate: {
        opacity: 1,
        y: 0
      }, transition: {
        delay: i * 0.05
      }, whileHover: {
        y: -4
      }, onClick: () => setSelected(f), className: "glass rounded-2xl p-6 text-left transition-shadow hover:shadow-[var(--shadow-glow)]", children: [
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-xs uppercase tracking-widest text-primary", children: [
          /* @__PURE__ */ jsx(FileText, { className: "h-3.5 w-3.5" }),
          f.type
        ] }),
        /* @__PURE__ */ jsx("h3", { className: "mt-2 text-lg font-semibold", children: f.title }),
        f.description && /* @__PURE__ */ jsx("p", { className: "mt-1 line-clamp-2 text-sm text-muted-foreground", children: f.description }),
        /* @__PURE__ */ jsxs("div", { className: "mt-4 flex items-center gap-3 text-xs text-muted-foreground", children: [
          /* @__PURE__ */ jsx(Clock, { className: "h-3.5 w-3.5" }),
          f.has_timer ? `${f.time_limit_minutes} min` : "No timer"
        ] })
      ] }, f.id)) })
    ] }),
    /* @__PURE__ */ jsxs("section", { className: "mx-auto mt-12 max-w-6xl", children: [
      /* @__PURE__ */ jsx("h2", { className: "mb-4 text-sm font-semibold uppercase tracking-widest text-muted-foreground", children: "Completed" }),
      completed.length === 0 ? /* @__PURE__ */ jsx("div", { className: "glass rounded-2xl p-6 text-center text-sm text-muted-foreground", children: "Your completed quizzes appear here." }) : /* @__PURE__ */ jsx("div", { className: "grid gap-4 sm:grid-cols-2 lg:grid-cols-3", children: completed.map((f) => {
        const sub = submissions.find((s) => s.form_id === f.id);
        const pct = Math.round(sub.score / Math.max(1, sub.total_questions) * 100);
        return /* @__PURE__ */ jsxs("button", { onClick: () => setSelected(f), className: "glass rounded-2xl p-6 text-left", children: [
          /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 text-xs uppercase tracking-widest text-success", children: [
            /* @__PURE__ */ jsx(CheckCircle2, { className: "h-3.5 w-3.5" }),
            " Completed"
          ] }),
          /* @__PURE__ */ jsx("h3", { className: "mt-2 text-lg font-semibold", children: f.title }),
          /* @__PURE__ */ jsxs("div", { className: "mt-4 flex items-baseline gap-2", children: [
            /* @__PURE__ */ jsxs("span", { className: "text-2xl font-bold text-gradient", children: [
              pct,
              "%"
            ] }),
            /* @__PURE__ */ jsxs("span", { className: "text-xs text-muted-foreground", children: [
              sub.score,
              " / ",
              sub.total_questions
            ] })
          ] })
        ] }, f.id);
      }) })
    ] }),
    /* @__PURE__ */ jsx(PreQuizModal, { form: selected, onClose: () => setSelected(null), alreadyCompleted: selected ? completedIds.has(selected.id) : false })
  ] });
}
function PreQuizModal({
  form,
  onClose,
  alreadyCompleted
}) {
  const [showLeaderboard, setShowLeaderboard] = useState(false);
  const fetchCount = useServerFn(getQuestionCount);
  const fetchLb = useServerFn(getLeaderboard);
  const {
    data: questionCount
  } = useQuery({
    queryKey: ["q-count", form?.id],
    enabled: !!form,
    queryFn: () => fetchCount({
      data: {
        formId: form.id
      }
    })
  });
  const {
    data: leaderboard = []
  } = useQuery({
    queryKey: ["lb", form?.id],
    enabled: !!form && showLeaderboard,
    queryFn: () => fetchLb({
      data: {
        formId: form.id
      }
    })
  });
  useEffect(() => {
    if (!form) setShowLeaderboard(false);
  }, [form]);
  return /* @__PURE__ */ jsx(Dialog, { open: !!form, onOpenChange: (o) => !o && onClose(), children: /* @__PURE__ */ jsx(DialogContent, { className: "glass-strong border-white/10 sm:max-w-lg", children: form && /* @__PURE__ */ jsxs(Fragment, { children: [
    /* @__PURE__ */ jsxs(DialogHeader, { children: [
      /* @__PURE__ */ jsx(DialogTitle, { className: "text-2xl", children: form.title }),
      /* @__PURE__ */ jsx(DialogDescription, { children: form.description ?? "Get ready to start." })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: "grid grid-cols-2 gap-3", children: [
      /* @__PURE__ */ jsxs("div", { className: "rounded-xl border border-white/10 bg-background/40 p-4", children: [
        /* @__PURE__ */ jsx("p", { className: "text-xs uppercase tracking-widest text-muted-foreground", children: "Questions" }),
        /* @__PURE__ */ jsx("p", { className: "mt-1 text-2xl font-bold", children: questionCount ?? "…" })
      ] }),
      /* @__PURE__ */ jsxs("div", { className: "rounded-xl border border-white/10 bg-background/40 p-4", children: [
        /* @__PURE__ */ jsx("p", { className: "text-xs uppercase tracking-widest text-muted-foreground", children: "Timer" }),
        /* @__PURE__ */ jsx("p", { className: "mt-1 text-2xl font-bold", children: form.has_timer ? `${form.time_limit_minutes}m` : "Off" })
      ] })
    ] }),
    /* @__PURE__ */ jsx(AnimatePresence, { children: showLeaderboard && /* @__PURE__ */ jsx(motion.div, { initial: {
      opacity: 0,
      height: 0
    }, animate: {
      opacity: 1,
      height: "auto"
    }, exit: {
      opacity: 0,
      height: 0
    }, className: "overflow-hidden", children: /* @__PURE__ */ jsxs("div", { className: "rounded-xl border border-white/10 bg-background/40 p-4 max-h-64 overflow-auto", children: [
      /* @__PURE__ */ jsx("p", { className: "mb-2 text-xs uppercase tracking-widest text-muted-foreground", children: "Leaderboard" }),
      leaderboard.length === 0 ? /* @__PURE__ */ jsx("p", { className: "text-sm text-muted-foreground", children: "No submissions yet." }) : /* @__PURE__ */ jsx("ol", { className: "space-y-2", children: leaderboard.map((row, i) => /* @__PURE__ */ jsxs("li", { className: "flex items-center justify-between text-sm", children: [
        /* @__PURE__ */ jsxs("span", { className: "flex items-center gap-3", children: [
          /* @__PURE__ */ jsxs("span", { className: `w-6 text-center font-mono ${i === 0 ? "text-accent" : "text-muted-foreground"}`, children: [
            "#",
            i + 1
          ] }),
          /* @__PURE__ */ jsx("span", { className: "font-medium", children: row.name })
        ] }),
        /* @__PURE__ */ jsxs("span", { className: "font-mono text-xs text-muted-foreground", children: [
          row.score,
          "/",
          row.total,
          " · ",
          formatTime(row.time)
        ] })
      ] }, i)) })
    ] }) }) }),
    /* @__PURE__ */ jsxs("div", { className: "mt-2 flex flex-col gap-2 sm:flex-row", children: [
      /* @__PURE__ */ jsxs(Button, { variant: "outline", onClick: () => setShowLeaderboard((s) => !s), className: "gap-2 border-white/10", children: [
        /* @__PURE__ */ jsx(Trophy, { className: "h-4 w-4" }),
        showLeaderboard ? "Hide" : "View",
        " Rankings"
      ] }),
      alreadyCompleted ? /* @__PURE__ */ jsx(Button, { disabled: true, className: "flex-1", children: "Already submitted" }) : /* @__PURE__ */ jsx(Button, { asChild: true, className: "flex-1 gap-2 bg-gradient-to-r from-primary to-accent text-primary-foreground glow", children: /* @__PURE__ */ jsxs(Link, { to: "/quiz/$id", params: {
        id: form.id
      }, children: [
        /* @__PURE__ */ jsx(PlayCircle, { className: "h-4 w-4" }),
        " Start Quiz"
      ] }) })
    ] })
  ] }) }) });
}
function formatTime(sec) {
  const m = Math.floor(sec / 60);
  const s = sec % 60;
  return `${m}:${s.toString().padStart(2, "0")}`;
}
export {
  StudentDashboard as component
};
